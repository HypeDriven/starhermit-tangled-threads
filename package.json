{
  "name": "tangled-threads",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "vitest run",
    "check": "node --check server.js && node --check src/rules.js && node --check src/content.js && node --check src/session.js && node --check src/audio.js && node --check src/ui.js && node --check src/main.js && node --check src/render.js",
    "smoke": "node tests/browser.smoke.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "vitest": "^4.1.11"
  }
}
